name: SAST XSS

on: [push]

jobs:
  sast:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - run: pip install semgrep

      - run: semgrep --config=semgrep/xss-rule.yaml .
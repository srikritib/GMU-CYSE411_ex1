name: Task 4 - SQL Injection

on:
  push:
    paths:
      - '**.js'

jobs:
  semgrep:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install semgrep
      - run: semgrep --config semgrep/task4.yaml --error